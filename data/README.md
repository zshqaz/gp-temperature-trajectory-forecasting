# Data directory

Place the source CSV here as `reanalysis-era5-single-levels-timeseries-sfcb7pyzsqo.csv`.

The CSV is deliberately excluded from Git. Retrieval and integrity instructions are in `docs/data_acquisition.md`.
