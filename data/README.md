# Data directory

Place the source CSV here as `reanalysis-era5-single-levels-timeseries-sfcb7pyzsqo.csv`, or pass its existing download path with the command-line `--data` option.

The CSV is deliberately excluded from Git. Retrieval, schema, and structural validation instructions are in `docs/data_acquisition.md`.
